%h1 Packages

%table
  %thead
    %tr
      %th Name
      %th Version
      %th Suggests
      %th Description
  %tbody
  - @packages.each do |package|
    %tr
      %td= package.name
      %td= package.version
      %td= package.suggests
      %td= package.description
